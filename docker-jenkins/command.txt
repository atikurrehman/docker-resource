sudo chown 1000:1000 $pwd/jenkind_home -R ->to giw permission on folder for jenkins
docker cp (name of file) (name of container):(location)->will copy file to container 
 ssh -i remote_user@remote_host->will connet to remote_host with user remote_user using ssh private key
